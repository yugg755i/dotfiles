-- programs.lua
-- Note: these aren't referenced by binds.lua currently (it hardcodes app
-- names directly). Kept here in case you wire them up later.
return {
	terminal = "kitty",
	fileManager = "nautilus",
	menu = "rofi -show drun",
}
