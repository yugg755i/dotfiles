#![enable(implicit_some)]
#![enable(unwrap_newtypes)]
#![enable(unwrap_variant_newtypes)]
(
    default_album_art_path: None,
    format_tag_separator: " | ",
    browser_column_widths: [20, 38, 42],
    background_color: None,
    text_color: Some("#cdc3ce"),
    header_background_color: None,
    modal_background_color: Some("#211f21"),
    modal_backdrop: true,
    preview_label_style: (fg: "#cfc2d3"),
    preview_metadata_group_style: (fg: "#d1c1d9", modifiers: "Bold"),
    tab_bar: (
        active_style: (fg: "#342e37", bg: "#cdc3ce", modifiers: "Bold"),
        inactive_style: (fg: "#cdc3ce", modifiers: "Bold"),
    ),
    highlighted_item_style: (fg: "#cdc3ce", modifiers: "Bold"),
    current_item_style: (fg: "#cdc3ce", bg: "#655b6a", modifiers: "Bold"),
    borders_style: (fg: "#484648"),
    highlight_border_style: (fg: "#cdc3ce"),
    symbols: (
        song: "",
        dir: "",
        playlist: "",
        marker: "",
        ellipsis: "...",
        song_style: (fg: "#cfc2d3"),
        dir_style: (fg: "#d1c1d9"),
        playlist_style: (fg: "#cdc3ce"),
        marker_style: (fg: "#ffb4ab", modifiers: "Bold"),
    ),
    level_styles: (
        info: (fg: "blue", bg: "black"),
        warn: (fg: "yellow", bg: "black"),
        error: (fg: "red", bg: "black"),
        debug: (fg: "light_green", bg: "black"),
        trace: (fg: "magenta", bg: "black"),
    ),
    progress_bar: (
        symbols: ["[", "-", ">", " ", "]"],
        track_style: (
            fg: "#484648",
        ),
        elapsed_style: (
            fg: "#cdc3ce",
        ),
        thumb_style: (
            fg: "#cdc3ce",
        ),
        use_track_when_empty: false,
    ),
    scrollbar: (
        symbols: ["│", "█", "▲", "▼"],
        track_style: (fg: "#484648"),
        ends_style: (fg: "#e9dfea"),
        thumb_style: (fg: "#cfc2d3"),
    ),
    song_table_format: [
        (
            prop: (
                kind: Property(Artist),
                style: (fg: "#ecdef0"),
                default: (kind: Text("Unknown"), style: (fg: "#ecdef0"))
            ),
            label_prop: (kind: Text("Artist"), style: (fg: "#d1c1d9", modifiers: "Bold")),
            width: "20%",
        ),
        (
            prop: (
                kind: Property(Title),
                style: (fg: "#cfc2d3"),
                default: (kind: Text("Unknown"), style: (fg: "#ecdef0"))
            ),
            label_prop: (kind: Text("Title"), style: (fg: "#d1c1d9", modifiers: "Bold")),
            width: "35%",
        ),
        (
            prop: (kind: Property(Album), style: (fg: "#ecdef0"),
                default: (kind: Text("Unknown Album"), style: (fg: "#ecdef0"))
            ),
            label_prop: (kind: Text("Album"), style: (fg: "#d1c1d9", modifiers: "Bold")),
            width: "30%",
        ),
        (
            prop: (kind: Property(Duration), style: (fg: "#cfc2d3"),
                default: (kind: Text("-"), style: (fg: "#ecdef0"))
            ),
            label_prop: (kind: Text("Duration"), style: (fg: "#d1c1d9", modifiers: "Bold")),
            width: "15%",
            alignment: Right,
        ),
    ],
    layout: Split(
        direction: Vertical,
        panes: [
            (
                size: "4",
                borders: "ALL",
                border_symbols: Inherited(parent: Rounded, bottom_left: "├", bottom_right: "┤"),
                pane: Split(
                    direction: Horizontal,
                    panes: [
                        (
                            size: "35",
                            pane: Component("header_left"),
                        ),
                        (
                            size: "100%",
                            pane: Component("header_center"),
                        ),
                        (
                            size: "35",
                            pane: Component("header_right"),
                        ),
                    ],
                ),
            ),
            (
                pane: Pane(Tabs),
                size: "3",
                borders: "LEFT | RIGHT | BOTTOM",
                border_symbols: Rounded,
            ),
            (
                pane: Pane(TabContent),
                size: "100%",
            ),
            (
                size: "3",
                pane: Split(
                    direction: Horizontal,
                    panes: [
                        (
                            size: "12",
                            borders: "ALL",
                            border_symbols: Inherited(parent: Rounded, top_right: "┬", bottom_right: "┴"),
                            pane: Component("input_mode"),
                        ),
                        (
                            size: "100%",
                            borders: "TOP | BOTTOM | RIGHT",
                            border_symbols: Rounded,
                            border_title: [
                                (kind: Text(" ")),
                                (kind: Property(Status(QueueLength()))),
                                (kind: Text(" tracks · ")),
                                (kind: Property(Status(QueueTimeTotal()))),
                                (kind: Text(" total ")),
                            ],
                            border_title_alignment: Right,
                            pane: Pane(ProgressBar),
                        ),
                    ]
                ),
            ),
        ],
    ),
    components: {
        "input_mode": Pane(Property(
            content: [
                (kind: Transform(Replace(content: (kind: Property(Status(InputMode()))), replacements: [
                    (match: "Normal", replace: (kind: Text(" NORMAL "), style: (fg: "#342e37", bg: "#cdc3ce", modifiers: "Bold"))),
                    (match: "Insert", replace: (kind: Text(" INSERT "), style: (fg: "#342e37", bg: "#d1c1d9", modifiers: "Bold"))),
                ])))
            ],
            align: Center,
        )),
        // --- Header, rebuilt with the current component/layout system ---
        // (theme.header no longer exists; header content is composed from
        // Property panes, same pattern rmpc's own default theme uses)
        "header_left": Split(
            direction: Vertical,
            panes: [
                (size: "1", pane: Component("header_state")),
                (size: "1", pane: Component("header_elapsed_bitrate")),
            ],
        ),
        "header_center": Split(
            direction: Vertical,
            panes: [
                (size: "1", pane: Component("header_title")),
                (size: "1", pane: Component("header_artist_album")),
            ],
        ),
        "header_right": Split(
            direction: Vertical,
            panes: [
                (size: "1", pane: Component("header_scan_volume")),
                (size: "1", pane: Component("header_states")),
            ],
        ),
        "header_state": Pane(Property(
            content: [
                (kind: Text("["), style: (fg: "#d1c1d9", modifiers: "Bold")),
                (
                    kind: Property(Status(StateV2(playing_label: "Playing", paused_label: "Paused", stopped_label: "Stopped"))),
                    style: (fg: "#d1c1d9", modifiers: "Bold")
                ),
                (kind: Text("]"), style: (fg: "#d1c1d9", modifiers: "Bold")),
            ],
            align: Left,
        )),
        "header_title": Pane(Property(
            content: [
                (kind: Property(Song(Title)), style: (modifiers: "Bold"),
                    default: (kind: Text("No Song"), style: (modifiers: "Bold"))
                )
            ],
            align: Center,
        )),
        "header_scan_volume": Pane(Property(
            content: [
                (kind: Property(Widget(ScanStatus)), style: (fg: "#cfc2d3")),
                (kind: Text(" ")),
                (kind: Property(Widget(Volume)), style: (fg: "#cfc2d3")),
            ],
            align: Right,
        )),
        "header_elapsed_bitrate": Pane(Property(
            content: [
                (kind: Property(Status(Elapsed))),
                (kind: Text(" / ")),
                (kind: Property(Status(Duration))),
                (kind: Text(" (")),
                (kind: Property(Status(Bitrate))),
                (kind: Text(" kbps)")),
            ],
            align: Left,
        )),
        "header_artist_album": Pane(Property(
            content: [
                (kind: Property(Song(Artist)), style: (fg: "#d1c1d9", modifiers: "Bold"),
                    default: (kind: Text("Unknown"), style: (fg: "#d1c1d9", modifiers: "Bold"))
                ),
                (kind: Text(" - ")),
                (kind: Property(Song(Album)),
                    style: (fg: "#cfc2d3"),
                    default: (kind: Text("Unknown Album"))
                ),
            ],
            align: Center,
        )),
        "header_states": Pane(Property(
            content: [
                (
                    kind: Property(Widget(States(
                        active_style: (fg: "#ffb4ab", modifiers: "Bold"),
                        separator_style: (fg: "#939091")))
                    ),
                    style: (fg: "#cdc3ce")
                ),
            ],
            align: Right,
        )),
    },
    browser_song_format: [
        (
            kind: Group([
                (kind: Property(Track), style: (fg: "#cfc2d3")),
                (kind: Text(" ")),
            ])
        ),
        (
            kind: Group([
                (kind: Property(Artist), style: (fg: "#cfc2d3")),
                (kind: Text(" - ")),
                (kind: Property(Title), style: (fg: "#cfc2d3")),
            ]),
            default: (kind: Property(Filename), style: (fg: "#cfc2d3"))
        ),
    ],
    lyrics: (
        timestamp: true
    ),
    cava: (

        bar_width: 2, // width of a single bar in columns
        bar_spacing: 1, // free space between bars in columns

        orientation: Bottom,

        // A simple color gradient.
        // This is a map where keys are percent values of the height where the
        // color starts. After that it is linearly interpolated towards the next
        // value. Keys must be between 0 and 100 and if the first or last key
        // are not 0 and 100 respectively, the lowest and highest value will be
        // used as 0 and 100.
        // Only hex and RGB colors are supported here and your terminal must
        // support them as well!
        // rmpc does not support `horizontal_gradient` (yet?)
        bar_color: Gradient({
            0: "#4c4452",
            50: "#cfc2d3",
            100: "#ecdef0"
        })
    ),
)
